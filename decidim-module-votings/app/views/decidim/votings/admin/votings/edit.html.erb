<div class="row column" >
  <%= t '.simulation_address' %>:
  <a href="<%= "#{resource_locator(voting).path}?key=#{voting.simulation_key}" %>"><%= "#{resource_locator(voting).path}?key=#{voting.simulation_key}" %></a>
</div>

<%=  decidim_form_for(@form, html: { class: 'form edit_voting' }) do |f| %>
    <%= render partial: 'form', object: f, locals: { title: t('.title') } %>

    <div class="button--double form-general-submit">
      <%= f.submit t('.update') %>
    </div>
<% end %>