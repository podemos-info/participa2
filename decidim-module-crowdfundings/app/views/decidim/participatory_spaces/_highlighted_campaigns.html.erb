<% if campaigns.any? %>
  <%= cell(
    "decidim/collapsible_list",
    campaigns,
    cell_options: { context: { current_user: current_user } },
    list_class: "column",
    size: campaigns.count
    ) %>
<% end %>
